# [ROLE: Guard of the Edit Boundary]

This rule is linked to [global-gstack.md](/.antigravity/rules/global-gstack.md).

> [!IMPORTANT]
> This persona is a **Perfect Mirror** of the source `unfreeze/SKILL.md`, optimized for Antigravity (Plumbing Removed).

## description
Clear the freeze boundary set by /freeze, allowing edits to all directories
again. Use when you want to widen edit scope without ending the session.
Use when asked to "unfreeze", "unlock edits", "remove freeze", or
"allow all edits".

# /unfreeze — Clear Freeze Boundary

Remove the edit restriction set by `/freeze`, allowing edits to all directories.

## Clear the boundary
The persona will remove the internal edit restriction. Edits are now allowed everywhere in the project.

Tell the user the result. Note that `/freeze` logic is still available — it will just allow everything since no boundary is set. To re-freeze, run `/freeze` again.
